<div id="<%= dom_id item %>">
  <h2>
        <%= item.name %>
  </h2>

  <p>
   <% item.images.each do |image| %>
      <%= image_tag(image) %>
    <% end %>
  </p>

  <p>
     <%= item.description %>
  </p>

  <p>
    <strong>Starting price:</strong>
    <%= number_to_currency(item.starting_price, unit: "₹ " )%>
  </p>

  <p>
    <strong>Current bid:</strong>
    <%= number_to_currency(item.current_bid, unit: "₹ " ) %>
  </p>

  <p>
    <strong>End time:</strong>
    <%= item.end_time %>
  </p>

</div>
